name: web_chart
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  csv: ^5.1.1
  file_picker: ^6.1.1
  fl_chart: ^0.66.0
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  html: ^0.15.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
